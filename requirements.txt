# requirements.txt - Updated for Single Container Deployment

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
websockets>=11.0.0
aiofiles>=23.0.0

# HTTP client and async support
aiohttp==3.9.1
httpx>=0.24.0

# System monitoring and utilities
psutil==5.9.6

# Static file serving (already included in FastAPI)
# Jinja2>=3.1.0  # For template rendering if needed

# Enhanced Features (Optional)
# Redis support
redis>=4.5.0
aioredis>=2.0.0

# Machine Learning features (for advanced routing)
sentence-transformers>=2.2.0
transformers>=4.21.0
huggingface-hub>=0.20.0,<0.25.0
scikit-learn>=1.1.0
scipy>=1.9.0
numpy>=1.21.0,<1.25.0

# Vector similarity search
faiss-cpu==1.7.4

# Streaming support
sse-starlette==1.6.5

# Security and authentication
PyJWT>=2.8.0
cryptography>=3.4.8

# Performance monitoring
prometheus-client>=0.19.0

# Logging and utilities
python-json-logger>=2.0.7
click>=8.0.0

# Development and testing (optional)
pytest>=7.1.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# CORS and security middleware (already in FastAPI)
# python-jose[cryptography]>=3.3.0  # For JWT handling

# Background tasks (already in FastAPI)
# celery>=5.2.0  # If you need distributed task queue

# Database support (if needed)
# sqlalchemy>=1.4.0
# alembic>=1.8.0

# File upload and processing
# python-magic>=0.4.27  # For file type detection

# Email support (if needed)
# fastapi-mail>=1.2.0

# Rate limiting (if needed)
# slowapi>=0.1.5

# Configuration and secrets management
# python-dotenv>=0.19.0  # For .env file support (already in pydantic-settings)
